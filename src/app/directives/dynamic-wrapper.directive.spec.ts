import { DynamicWrapperDirective } from './dynamic-wrapper.directive';

describe('DynamicWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
